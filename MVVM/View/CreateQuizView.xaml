<UserControl x:Class="Quizy.MVVM.View.CreateQuizView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Quizy.MVVM.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="5*"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="QuestionInput" Text="{Binding Question}" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2"
                 Margin="40,30,40,30" Style="{StaticResource QuestionInputTheme}"/>
        <StackPanel Orientation="Horizontal" Grid.Row="1" Grid.Column="0" HorizontalAlignment="Center">
            <TextBox x:Name="Answear1Input" Text="{Binding Answers[0]}" Style="{StaticResource AnswearInputTheme}"/>
            <ToggleButton IsChecked="{Binding Checked[0]}" VerticalAlignment="Center" Margin="10" Style="{StaticResource AnswerButtonTheme}"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Grid.Row="1" Grid.Column="1" HorizontalAlignment="Center">
            <TextBox x:Name="Answear2Input" Text="{Binding Answers[1]}" Style="{StaticResource AnswearInputTheme}"/>
            <ToggleButton IsChecked="{Binding Checked[1]}" VerticalAlignment="Center" Margin="10" Style="{StaticResource AnswerButtonTheme}"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Grid.Row="2" Grid.Column="0" HorizontalAlignment="Center">
            <TextBox x:Name="Answear3Input" Text="{Binding Answers[2]}" Style="{StaticResource AnswearInputTheme}"/>
            <ToggleButton IsChecked="{Binding Checked[2]}" VerticalAlignment="Center" Margin="10" Style="{StaticResource AnswerButtonTheme}"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Grid.Row="2" Grid.Column="1" HorizontalAlignment="Center">
            <TextBox x:Name="Answear4Input" Text="{Binding Answers[3]}" Style="{StaticResource AnswearInputTheme}"/>
            <ToggleButton IsChecked="{Binding Checked[3]}" VerticalAlignment="Center" Margin="10" Style="{StaticResource AnswerButtonTheme}"/>
        </StackPanel>

        <Button Style="{StaticResource NextButton}" Content="->" Grid.Column="1" Grid.Row="3" Command="{Binding Add}"/>


    </Grid>
</UserControl>
