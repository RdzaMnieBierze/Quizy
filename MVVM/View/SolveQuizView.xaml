<UserControl x:Class="Quizy.MVVM.View.SolveQuizView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Quizy.MVVM.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        
    <Grid Visibility="{Binding IsResult, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>

        <ToggleButton Content="{Binding Answer1}" Style="{StaticResource Answer1SolveButtonTheme}"  IsChecked="{Binding IsAnswer1Selected}" Grid.Column="0" Grid.Row="1"/>
        <ToggleButton Content="{Binding Answer2}" Style="{StaticResource Answer1SolveButtonTheme}" IsChecked="{Binding IsAnswer2Selected}" Grid.Column="1" Grid.Row="1"/>
        <ToggleButton Content="{Binding Answer3}" Style="{StaticResource Answer1SolveButtonTheme}" IsChecked="{Binding IsAnswer3Selected}" Grid.Column="0" Grid.Row="2"/>
        <ToggleButton Content="{Binding Answer4}" Style="{StaticResource Answer1SolveButtonTheme}"  IsChecked="{Binding IsAnswer4Selected}" Grid.Column="1" Grid.Row="2"/>
        
       

        <TextBlock Text="{Binding Question}" FontSize="40" TextAlignment="Center" VerticalAlignment="Stretch" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2"/>
        <TextBlock Text="{Binding Timer}" FontSize="30" TextAlignment="Center" VerticalAlignment="Stretch" Grid.Row="3" Grid.Column="0"/>

        <Grid Grid.Row="3" Grid.Column="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
            <Button Command="{Binding NextQuestionCommand}" Content="->" Grid.Column="1" Style="{StaticResource NextButton}"/>
            <Button Command="{Binding EndQuizCommand}" Content="Zakończ" Grid.Column="2" Style="{StaticResource NextButton}"/>
                    <Button Command="{Binding ReadQuizCommand}" Content="{Binding EndLoadButtonContent}" Grid.Column="0" Style="{StaticResource NextButton}"/>
            </Grid>

       
    </Grid>
        <Grid Visibility="{Binding IsResult, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="5*"/>
            </Grid.RowDefinitions>
            <ListBox Grid.Row="1" Margin="10"  ItemsSource="{Binding Questions.Questions}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="5">
                            <!-- Treść pytania -->
                            <TextBlock FontWeight="Bold"
                           Text="{Binding Content}" />

                            <!-- Poprawne odpowiedzi -->
                            <ItemsControl ItemsSource="{Binding Converter={StaticResource CorrectAnswersConverter}}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock><Run Text="• " /><Run Text=" "/><Run Text="{Binding Content}" /></TextBlock>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <TextBlock Text="{Binding Result}" FontSize="30" TextAlignment="Center" VerticalAlignment="Stretch"/>

        </Grid>
        
    </Grid>
</UserControl>
